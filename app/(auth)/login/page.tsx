// import { Login } from '@/pages/Login/ui/Login';

import { Login } from '@/pages/Login/ui/Login';

interface pageProps {
	className?: string;
}

const LoginPage = ({}: pageProps) => {
	return <Login></Login>;
};

export default LoginPage;
